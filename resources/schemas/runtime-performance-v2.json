{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:laravel-guard:schema:runtime-performance:2",
  "title": "Laravel Guard runtime performance report v2",
  "type": "object",
  "required": ["schema", "schema_version", "scenario", "runs", "operations_per_run", "average_us", "p95_us", "peak_memory_mb", "memory_growth_mb", "state_leaks", "status", "violations"],
  "properties": {
    "schema": {"const": "laravel-guard/runtime-performance"},
    "schema_version": {"const": 2},
    "scenario": {"enum": ["query", "upload", "worker"]},
    "runs": {"type": "integer", "minimum": 1, "maximum": 100},
    "operations_per_run": {"type": "integer", "minimum": 1, "maximum": 1000000},
    "average_us": {"type": "number", "minimum": 0},
    "p95_us": {"type": "number", "minimum": 0},
    "peak_memory_mb": {"type": "number", "minimum": 0},
    "memory_growth_mb": {"type": "number", "minimum": 0},
    "state_leaks": {"type": "integer", "minimum": 0},
    "status": {"enum": ["pass", "fail"]},
    "violations": {"type": "array", "items": {"type": "string"}}
  },
  "additionalProperties": false
}